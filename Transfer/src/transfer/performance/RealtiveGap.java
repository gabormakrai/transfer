package transfer.performance;

import transfer.graph.base.Graph;
import transfer.tap.base.Demand;

public class RealtiveGap implements AbstractPerformance {
	@Override
	public double calculate(Graph graph, Demand[] demands) {
		return 0;
	}
}
